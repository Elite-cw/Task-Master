@extends('layouts.app')
@section('content')
<div class="card" style="max-width:560px;margin:52px auto;text-align:center"><p class="eyebrow" style="color:#6967cc">One more step</p><h1>Verify your email</h1><p class="meta">We sent a verification link to <strong>{{ auth()->user()->email }}</strong>. Open it to unlock your workspace.</p><form method="POST" action="{{ route('verification.send') }}">@csrf<div class="actions" style="justify-content:center"><button class="btn secondary">Resend verification email</button></div></form>@if(app()->environment('local'))<div style="margin-top:28px;padding-top:22px;border-top:1px solid var(--line)"><p class="meta"><strong>Local assignment mode</strong><br>SMTP is unavailable, so use this demo verification code:</p><form method="POST" action="{{ route('verification.demo') }}">@csrf<label style="text-align:left">Demo code</label><input name="code" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" placeholder="000000" required><div class="actions" style="justify-content:center"><button class="btn">Verify with demo code</button></div></form></div>@endif<form method="POST" action="{{ route('logout') }}">@csrf<div class="actions" style="justify-content:center"><button class="btn small secondary">Sign out</button></div></form></div>
@endsection
